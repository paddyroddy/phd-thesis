% ---------------------------
%% renew commands first
% ---------------------------

% fixed size absolute
\renewcommand{\abs}[1]{\big\lvert#1\big\rvert}

% divergence
\renewcommand{\div}{\text{div}}

% gradient
\renewcommand{\grad}[1]{\nabla#1}

% fixed size 2-norm
\renewcommand{\norm}[1]{{\big\lVert#1\big\rVert}_{2}}

% ---------------------------
%% then new commands
% ---------------------------

% nice tilde
\newcommand{\almost}[1]{{\thicksim}#1}

% axisymmetric spherical harmonic indices
\newcommand{\axiHarmonic}[2][]{{#2}_{\ell#1 0}}

% bispectrum
\newcommand{\bispectrum}[1]{B_{#1}(k_{1},k_{2},k_{3})} % chktex 36

% conjugate
\newcommand{\conj}[1]{#1^{\ast}}

% sifting convolution of two functions
\newcommand{\convolution}[2]{(#1 \circledcirc{} #2)}

% continuous wavelet transform
\newcommand{\cwt}[1]{\text{CWT}_{#1}(s,\tau)} % chktex 36

% diagonal matrix
\newcommand{\diag}[1]{\text{diag}\{#1\}} % chktex 21

% Slepian matrix
\newcommand{\Dmatrix}{D_{\ell{} m,\ell' m'}}

% edges
\newcommand{\edges}{\mathcal{E}}

% faces
\newcommand{\faces}{\mathcal{F}}

% spherical harmonic factor
\newcommand{\factor}[1][]{\frac{2\ell#1+1}{4\pi}}

% Fourier space
\newcommand{\fourier}[1]{\widetilde{#1}}

% FWHM
\newcommand{\fwhm}[1]{\text{FWHM}=\SI{#1}{\degree}}

% graph
\newcommand{\graph}{\mathcal{G}}

% spherical harmonic indices
\newcommand{\harmonic}[2][]{{#2}_{\ell#1 m#1}}

% spherical harmonic sum
\newcommand{\harmonicSum}[1][]{\displaystyle\sum\limits_{\ell#1 m#1}}

% Hermitian conjugate
\newcommand{\hermitian}[1]{#1^{\dagger}}

% Hilbert space
\newcommand{\hilbert}[1]{L^{2}(#1)} % chktex 36

% max harmonic mesh index
\newcommand{\imax}{i_{\text{max}}}

% integrate over the manifold
\newcommand{\integrateManifold}[1]{\displaystyle\int\limits_{\mathcal{M}} \dd{#1}}

% integrate over the region
\newcommand{\integrateRegion}[1]{\displaystyle\int\limits_{R} #1}

% intergral over SO3
\newcommand{\integrateRotation}[1]{\displaystyle\int\limits_{\text{SO(3)}} \dd{\varrho(#1)}} % chktex 36

% integral over S2
\newcommand{\integrateSphere}[1]{\displaystyle\int\limits_{\mathbb{S}^{2}} \dd{\Omega(#1)}}

% inverse spherical harmonic factor
\newcommand{\inverseFactor}[1][]{\frac{4\pi}{2\ell#1+1}}

% Laplace operator
\newcommand{\laplace}[1]{\Delta_{\mathcal{#1}}}

% manifold
\newcommand{\manifold}{\mathcal{M}}

% mean
\newcommand{\mean}[1]{\overline{#1}}

% pixel space on mesh
\newcommand{\mesh}[1]{#1(x)}
\newcommand{\meshY}[1]{#1(y)}

% summing in harmonic space of the mesh
\newcommand{\meshSum}{\displaystyle\sum\limits_{i}}
\newcommand{\meshYSum}{\displaystyle\sum\limits_{j}}

% volume element of the mesh
\newcommand{\meshVolume}{\dd{x}}

% real space
\newcommand{\pixel}[2][]{#2(\omega#1)}

% phase factor
\newcommand{\phaseFactor}[1][]{{(-1)}^{m#1}}

% power spectrum
\newcommand{\powerSpectrum}{C_{\ell}}
\newcommand{\powerSpectrumDim}[1]{P_{#1}(k)} % chktex 36
\newcommand{\powerSpectrumHat}{\widehat{C}_{\ell}}
\newcommand{\powerSpectrumShell}[1]{\Delta_{#1}^{2}(k)} % chktex 36

% real positive parameter
\newcommand{\realPosParam}{\mathbb{R}_{\ast}^{+}}

% reduced Wigner D matrices
\newcommand{\reducedWigner}[4][]{d^{#2#1}_{#3#4}}

% rotated function
\newcommand{\rotation}[1]{\mathcal{R}_{#1}}

% SO3
\newcommand{\rotationGroup}{\text{SO(3)}} % chktex 36

% rotation matrices
\newcommand{\rotationMatrix}[1][]{\vb*{R}_{\rho#1}}

% rotation volume element
\newcommand{\rotationVolume}{\dd{\varrho(\rho)}}

% set of inputs
\newcommand{\set}[1]{\{#1\}} % chktex 21

% Slepian coefficients
\newcommand{\slepian}[2][]{{#2}_{p#1}}

% Slepian sum
\newcommand{\slepianSum}[1][]{\displaystyle\sum\limits_{p#1}}

% signal-to-noise
\newcommand{\snr}[1]{\text{SNR}(#1)} % chktex 36

% bandlimited subspace
\newcommand{\spaceBandlimit}{\mathcal{S}_{L}}

% spacelimited subspace
\newcommand{\spaceSpacelimit}{\mathcal{S}_{R}}

% sphere volume element
\newcommand{\sphereVolume}[1][]{\dd{\Omega(\omega#1)}}

% translated function
\newcommand{\translation}[1]{\mathcal{T}_{#1}}

% trispectrum
\newcommand{\trispectrum}[1]{T_{#1}(k_{1},k_{2},k_{3},k_{4},\abs{\vb*{k}_{1}+\vb*{k}_{2}},\abs{\vb*{k}_{2}+\vb*{k}_{3}})} % chktex 36

% S2
\newcommand{\twoSphere}{\mathbb{S}^{2}}

% variance
\newcommand{\variance}[1]{{\big[\Delta#1\big]}^{2}}

% vertices
\newcommand{\vertices}{\mathcal{V}}

% Wigner D matrices
\newcommand{\wigner}[4][]{D^{#2#1}_{#3#4}}

% third sum in Wigner D
\newcommand{\wignerSum}[1][]{\displaystyle\sum\limits_{n#1=-\ell#1}^{\ell#1}}

% sum over wavelet coefficients
\newcommand{\waveletSum}{\displaystyle\sum\limits_{j=J_{0}}^{J}}
